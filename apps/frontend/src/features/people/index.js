export { default } from './components/PeopleCatalog.jsx'
// Jobs assigns supervisors, so the roster is part of this feature's public
// surface — not just its screen.
export { initialPeople, emptyPerson, personTypeOptions } from './data/people.js'
